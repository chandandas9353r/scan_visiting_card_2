name: scan_visiting_card
description: Scan Visiting Cards and fetch Details in Soft Copy Form.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.1.0
  fluttertoast: ^8.2.2
  flutter_email_sender: ^6.0.1
  http_parser: ^4.0.2
  edge_detection: ^1.1.2
  permission_handler: ^10.0.0
  path_provider: ^2.0.11
  path: ^1.8.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true